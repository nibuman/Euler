bool ispal(int current); //checks to see if a 5 or 6 digit integer is a palindrome
bool *primeSoE(const int limit); //returns pointer to a list of primes
int modulo(long long int testnum, int modnum); //returns testnum (modulo modnum) e.g. modulo(16, 7) = 2
int multorder(long long int testnum, int modnum); //returns the multiplicative order
long long int gcd(long long int a, long long int b); //returns the greatest common divisor